{
  "name": "typeshape",
  "version": "0.0.4",
  "description": "DSL for runtime type checking",
  "main": "dist/typeshape.js",
  "author": "Dan Prince",
  "license": "MIT",
  "module": "dist/typeshape.es.js",
  "devDependencies": {
    "babel-preset-es2015": "^6.24.1",
    "jest": "^20.0.4",
    "jest-babel": "^1.0.1",
    "rollup": "^0.43.0",
    "rollup-plugin-buble": "^0.15.0"
  },
  "scripts": {
    "test": "jest",
    "clean": "rm -rf dist",
    "prebuild": "npm run clean",
    "build": "rollup -c",
    "prepublish": "npm run build"
  }
}
